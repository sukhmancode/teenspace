services:
  - type: web
    name: teenspace-api
    runtime: node
    region: oregon
    # install devDependencies during build so tools like `tsx` are available
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        scope: build,runtime
      - key: SESSION_SECRET
        scope: runtime
      - key: PORT
        value: 10000
    autoDeploy: false
